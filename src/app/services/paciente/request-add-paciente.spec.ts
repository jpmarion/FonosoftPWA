import { RequestAddPaciente } from './request-add-paciente';

describe('RequestAddPaciente', () => {
  it('should create an instance', () => {
    expect(new RequestAddPaciente()).toBeTruthy();
  });
});
