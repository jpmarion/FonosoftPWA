import { DataSourceTipoContacto } from './data-source-tipo-contacto';

describe('DataSourceTipoContacto', () => {
  it('should create an instance', () => {
    expect(new DataSourceTipoContacto()).toBeTruthy();
  });
});
